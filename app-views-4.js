function renderKnowledge(){const decisions=combinedCollection('decisions').filter(item=>matchesSearch(item.id,item.title,item.reason,item.decision,item.impact,item.status)),questions=combinedCollection('questions').filter(item=>matchesSearch(item.id,item.title,item.impact,item.answerOwner,item.status)),ideas=combinedCollection('ideas').filter(item=>matchesSearch(item.id,item.title,item.description,item.status)),timeline=combinedTimeline().filter(item=>LOCAL.ui.timelineType==='all'||item.type===LOCAL.ui.timelineType).filter(item=>matchesSearch(item.id,item.title,item.description,item.type,item.date)),goals=DATA.goals;return`${viewHeader('Entscheidungen & Wissen','Ziel, Scope, offene Fragen, Ideen und Timeline als zusammenhängender Projektkontext.',`<button class="button button-primary" data-open-dialog="decision">Entscheidung dokumentieren</button>`)}<div class="two-column-grid"><section class="panel"><p class="eyebrow">Projektziel</p><h3>${esc(goals.goal||DATA.description)}</h3><p>${esc(goals.problem||'')}</p><div class="kv"><strong>Zielgruppe</strong><span>${esc(goals.targetGroup||'—')}</span></div><details class="details"><summary>Erfolgskriterien</summary>${listMarkup(goals.successCriteria)}</details><details class="details"><summary>Nicht-Ziele</summary>${listMarkup(goals.nonGoals)}</details><details class="details"><summary>Einschränkungen</summary>${listMarkup(goals.constraints)}</details></section><section class="panel"><div class="section-title"><div><p class="eyebrow">Offene Punkte</p><h3>Fragen und Ideen</h3></div><div class="inline-actions"><button class="button button-small" data-open-dialog="question">Frage</button><button class="button button-small" data-open-dialog="idea">Idee</button></div></div>${questions.map(renderQuestionCard).join('')||emptyState('Keine Fragen.')}${ideas.map(renderIdeaCard).join('')||''}</section></div><section class="stack-section"><div class="section-title"><div><p class="eyebrow">Entscheidungslog</p><h3>Entscheidungen</h3></div><span class="badge">${decisions.length}</span></div>${decisions.map(renderDecisionCard).join('')||emptyState('Keine Entscheidungen.')}</section><section class="panel"><div class="section-title"><div><p class="eyebrow">Integrierte Timeline</p><h3>Projektverlauf</h3></div><select id="timelineTypeFilter"><option value="all">Alle Typen</option>${[...new Set(combinedTimeline().map(item=>item.type).filter(Boolean))].map(type=>`<option value="${esc(type)}" ${LOCAL.ui.timelineType===type?'selected':''}>${esc(type)}</option>`).join('')}</select></div><div class="timeline-list">${timeline.map(renderTimelineItem).join('')||emptyState('Keine Timeline-Einträge.')}</div></section>`}
function renderDecisionCard(item){return`<article class="panel compact-card"><div class="item-head"><div><div class="meta-row"><span class="badge">${esc(item.id)}</span><span class="badge">${esc(item.date||'')}</span></div><h4>${esc(item.title)}</h4></div>${statusBadge(item.status)}</div><div class="kv"><strong>Grund</strong><span>${esc(item.reason||'—')}</span></div><div class="kv"><strong>Entscheidung</strong><span>${esc(item.decision||'—')}</span></div><div class="kv"><strong>Auswirkung</strong><span>${esc(item.impact||'—')}</span></div><select data-entity-status="decisions" data-entity-id="${esc(item.id)}">${selectOptions([['aktiv','Aktiv'],['review','Review nötig'],['superseded','Ersetzt'],['archived','Archiviert']],item.status)}</select></article>`}
function renderQuestionCard(item){return`<article class="knowledge-card"><div class="item-head"><div><span class="id-label">${esc(item.id)}</span><h4>${esc(item.title)}</h4></div>${statusBadge(item.status)}</div><p>${esc(item.impact||'')}</p><textarea data-question-answer="${esc(item.id)}" placeholder="Lokale Antwort / Arbeitsstand">${esc(item.finalAnswer||'')}</textarea></article>`}
function renderIdeaCard(item){return`<article class="knowledge-card"><div class="item-head"><div><span class="id-label">${esc(item.id)}</span><h4>${esc(item.title)}</h4></div>${statusBadge(item.status)}</div><p>${esc(item.description||'')}</p></article>`}
function renderTimelineItem(item){return`<article class="timeline-entry"><span class="timeline-dot"></span><div><div class="meta-row"><span class="badge">${esc(item.type||'event')}</span><span>${compactDate(item.date)}</span></div><strong>${esc(item.title)}</strong><p>${esc(item.description||'')}</p>${item.targetView?`<button class="text-link" data-project-view-jump="${esc(item.targetView)}">Zugehörigen Bereich öffnen →</button>`:''}</div></article>`}
function renderFiles(){const files=DATA.files.filter(item=>matchesSearch(item.id,item.name,item.path,item.type,item.purpose,item.status)),backups=mergeById(LOCAL.backups,DATA.backups).filter(item=>matchesSearch(item.id,item.type,item.projectState,item.topics,item.path,item.status)),workflowFiles=DATA.workflowState.workflowFiles,repoPolicy=WORKSPACE.global.repositoryPolicy;return`${viewHeader('Dateien & Backups','Projektdateien, Workflow-Dokumente, Chat-Backups und sichere Export-/Import-Wege.',`<button class="button button-primary" data-project-export>Projekt exportieren</button>`)}<section class="privacy-banner privacy-warning"><div><strong>Private Repository empfohlen</strong><p>${esc(WORKSPACE.global.privacyNotice)}</p></div><span class="badge badge-warning">${esc(DATA.visibility||'private_repo_recommended')}</span></section><div class="backup-action-grid"><section class="panel"><p class="eyebrow">Nur dieses Projekt</p><h3>Projekt-Backup</h3><p>Exportiert Projektmetadaten, lokalen Status, lokale Notizen und zugeordnete Screenshots – ohne andere Projekte.</p><div class="inline-actions"><button class="button button-primary" data-project-export>Exportieren</button><label class="button button-secondary file-button">Importieren<input type="file" accept="application/json" data-project-import></label></div></section><section class="panel"><p class="eyebrow">Gesamter Workspace</p><h3>Globales Backup</h3><p>Exportiert alle Projekte, lokalen Status, Konfliktkopien und alle lokalen Screenshots.</p><div class="inline-actions"><button class="button button-secondary" data-global-export>Global exportieren</button><label class="button button-ghost file-button">Global importieren<input type="file" accept="application/json" data-global-import></label></div></section></div><div class="two-column-grid"><section class="panel"><div class="section-title"><div><p class="eyebrow">Projektdateien</p><h3>Dateiregister</h3></div><span class="badge">${files.length}</span></div>${files.map(file=>`<div class="file-row"><div><strong>${esc(file.name)}</strong><code>${esc(file.path||'')}</code><p>${esc(file.purpose||'')}</p></div>${statusBadge(file.status)}</div>`).join('')||emptyState('Keine Dateien.')}</section><section class="panel"><div class="section-title"><div><p class="eyebrow">Backups</p><h3>Referenzen</h3></div><button class="button button-small" data-open-dialog="backup">Eintrag</button></div>${backups.map(backup=>`<div class="file-row"><div><strong>${esc(backup.id)}</strong><span>${compactDate(backup.date)}</span><p>${esc(backup.projectState||backup.type||'')}</p><code>${esc(backup.path||'lokal')}</code></div>${statusBadge(backup.status)}</div>`).join('')||emptyState('Keine Backups dokumentiert.')}</section></div><section class="panel"><div class="section-title"><div><p class="eyebrow">Standardworkflow</p><h3>Workflow-Dateien</h3></div><span class="badge">${workflowFiles.length}</span></div><div class="workflow-file-grid">${workflowFiles.map(item=>`<div class="workflow-file"><div><strong>${esc(item.name)}</strong><span>${esc(item.id)}</span></div><div>${priorityBadge(item.priority)}${statusBadge(item.status)}</div></div>`).join('')}</div></section><section class="panel"><div class="section-title"><div><p class="eyebrow">Commit-Regeln</p><h3>Öffentlich sicher vs. privat</h3></div></div><div class="two-column-grid"><div><strong>Sicher zu committen</strong>${listMarkup(repoPolicy.safeToCommit)}</div><div><strong>Nur lokal oder privat</strong>${listMarkup(repoPolicy.localOrPrivateOnly)}</div></div></section>`}

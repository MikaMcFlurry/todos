function renderData(){refreshUnmappedLegacy();const unknownTodo=Object.keys(LOCAL.unmappedLegacyItems.todoState),unknownTests=Object.keys(LOCAL.unmappedLegacyItems.testState),notes=LOCAL.quickNotes.filter(item=>!item.archived&&matchesSearch(item.id,item.text,item.type,item.createdAt));return`${viewHeader('Daten & lokale Notizen','Validierung, Datenquellen, Legacy-Schutz, lokale Schnellnotizen und technische Grenzen.')}<div class="data-grid"><section class="panel ${VALIDATION.errors.length?'panel-danger':VALIDATION.warnings.length?'panel-warning':'panel-success'}"><div class="section-title"><div><p class="eyebrow">Validierung</p><h3>project-data.json</h3></div>${VALIDATION.errors.length?statusBadge(`${VALIDATION.errors.length} Fehler`):VALIDATION.warnings.length?statusBadge(`${VALIDATION.warnings.length} Hinweise`):statusBadge('Valide')}</div>${VALIDATION.errors.length?`<strong>Fehler</strong>${listMarkup(VALIDATION.errors)}`:''}${VALIDATION.warnings.length?`<strong>Hinweise</strong>${listMarkup(VALIDATION.warnings.slice(0,30))}`:''}</section><section class="panel"><p class="eyebrow">Quellen</p><h3>Datenebenen</h3><div class="kv"><strong>Workspace</strong><span>${esc(WORKSPACE.sourceInfo.workspaceData)}</span></div><div class="kv"><strong>Legacy</strong><span>${esc(WORKSPACE.sourceInfo.legacyData)}</span></div><div class="kv"><strong>Projekt-ID</strong><code>${esc(DATA.id)}</code></div><div class="kv"><strong>Screenshots</strong><span id="screenshotCount">wird ermittelt …</span></div></section><section class="panel"><p class="eyebrow">Legacy-Schutz</p><h3>Unbekannte IDs</h3><div class="kv"><strong>Todo/Task</strong><span>${unknownTodo.length}</span></div><div class="kv"><strong>Tests</strong><span>${unknownTests.length}</span></div>${unknownTodo.length?`<details class="details"><summary>Task-IDs</summary><code>${esc(unknownTodo.join(', '))}</code></details>`:''}${unknownTests.length?`<details class="details"><summary>Test-IDs</summary><code>${esc(unknownTests.join(', '))}</code></details>`:''}</section></div><section class="panel"><div class="section-title"><div><p class="eyebrow">Schnellnotizen</p><h3>Private lokale Notizen</h3></div><button class="button button-small" id="newQuickNoteButton">Speichern</button></div><div class="note-compose"><textarea id="quickNoteText" placeholder="Private Notiz …"></textarea><select id="quickNoteType"><option value="note">Notiz</option><option value="task">Mika-Aufgabe</option><option value="idea">Idee</option><option value="question">Frage</option></select></div>${notes.map(note=>`<div class="compact-row"><div><strong>${esc(note.text)}</strong><span>${esc(note.type)} · ${compactDate(note.createdAt)}</span></div><button class="button button-small button-ghost" data-archive-note="${esc(note.id)}">Archivieren</button></div>`).join('')||emptyState('Noch keine Schnellnotizen.')}</section><section class="panel panel-warning"><div class="section-title"><div><p class="eyebrow">Legacy</p><h3>Allgemeine private Notizen</h3></div><span class="badge badge-warning">nur lokal</span></div><p>Dieses Feld bewahrt alte Backup-Inhalte und wird nicht in öffentliche Projektdaten geschrieben.</p><textarea id="legacyNotes">${esc(LOCAL.legacyGeneralNotes)}</textarea></section><section class="panel"><div class="section-title"><div><p class="eyebrow">Importhistorie</p><h3>Letzte Zusammenführungen</h3></div><span class="badge">${LOCAL.importHistory.length}</span></div>${LOCAL.importHistory.slice(0,10).map(item=>`<div class="compact-row"><div><strong>${esc(item.schema||'unbekannt')}</strong><span>${compactDate(item.importedAt)} · ${esc(item.fileName||'')}</span><p>${esc(item.summary||'')}</p></div></div>`).join('')||emptyState('Noch kein Projekt-Import.')}</section><section class="panel panel-danger"><h3>Status zurücksetzen</h3><p>Nur lokale Task-/Teststatus werden zurückgesetzt. Notizen und Screenshots bleiben erhalten. Vorher exportieren.</p><button class="button button-danger" id="resetStatusesButton">Lokale Status zurücksetzen</button></section>`}
function renderProjectView(){const views={dashboard:renderProjectDashboard,workflow:renderWorkflow,work:renderWork,quality:renderQuality,ai:renderAI,knowledge:renderKnowledge,files:renderFiles,data:renderData};return(views[LOCAL.ui.activeView]||views.dashboard)()}
function renderApp(){$('#loadingState')?.classList.add('hidden');$('#errorState')?.classList.add('hidden');document.body.classList.toggle('project-open',Boolean(DATA));document.body.classList.toggle('workspace-open',!DATA);document.body.dataset.context=DATA?'project':'workspace';const title=$('#appTitle'),subtitle=$('#appSubtitle');if(DATA){title.textContent=DATA.name;subtitle.textContent=`${DATA.phase||DATA.statusLabel||''} · ${DATA.visibility==='private_repo_recommended'?'Private Repo empfohlen':'lokale Daten getrennt'}`}else{title.textContent=WORKSPACE.global.toolName||'KI-Projektmanagement';subtitle.textContent='Multi-Projekt-Workspace · Projekt auswählen'}const badge=$('#validationBadge');badge.textContent=VALIDATION.errors.length?`${VALIDATION.errors.length} Datenfehler`:VALIDATION.warnings.length?`${VALIDATION.warnings.length} Hinweise`:'Daten valide';badge.className=`badge ${VALIDATION.errors.length?'badge-danger':VALIDATION.warnings.length?'badge-warning':'badge-success'}`;$('#backToProjectsButton')?.classList.toggle('hidden',!DATA);$('#openMenuButton')?.classList.toggle('hidden',!DATA);$('#projectUtilityActions')?.classList.toggle('hidden',!DATA);$('#globalUtilityActions')?.classList.toggle('hidden',Boolean(DATA));$('#projectSidebar')?.classList.toggle('hidden',!DATA);$('#projectBottomNav')?.classList.toggle('hidden',!DATA);const sidebarName=$('#sidebarProjectName'),drawerName=$('#drawerProjectName');if(sidebarName)sidebarName.textContent=DATA?.shortName||DATA?.name||'—';if(drawerName)drawerName.textContent=DATA?.name||'Bereiche & Werkzeuge';$('#globalSearch').value=currentSearch();$('#mobileSearch').value=currentSearch();renderProjectNavigation();$('#viewRoot').innerHTML=DATA?renderProjectView():renderWorkspaceDashboard();bindViewEvents()}

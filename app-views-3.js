function renderTestCard(test){const result=testResult(test.id);return`<article class="panel test-card" data-status="${esc(result.status)}"><div class="item-head"><div><div class="meta-row"><span class="badge badge-blue">${esc(test.id)}</span><span class="badge">${esc(test.suite||'')}</span><span class="badge">${esc(test.when||'')}</span></div><h4>${esc(test.title)}</h4><p>${esc(test.suiteTitle||'')}</p></div>${statusBadge(TEST_STATUSES.find(item=>item[0]===result.status)?.[1]||result.status)}</div><div class="test-grid"><div><details class="details" open><summary>Schritte und Erwartung</summary><div class="instruction-grid"><div><strong>Schritte</strong><ol>${asArray(test.steps).map(step=>`<li>${esc(step)}</li>`).join('')}</ol></div><div><strong>Erwartung</strong><ul>${asArray(test.expected).map(step=>`<li>${esc(step)}</li>`).join('')}</ul></div></div></details><div class="screenshot-grid" data-screenshot-grid="${esc(test.id)}"></div></div><div class="test-controls"><label><span class="field-label">Status</span><select data-test-field="status" data-test-id="${esc(test.id)}">${selectOptions(TEST_STATUSES,result.status)}</select></label><div class="three-fields"><label><span class="field-label">Gerät</span><select data-test-field="device" data-test-id="${esc(test.id)}">${selectOptions(DEVICE_OPTIONS,result.device)}</select></label><label><span class="field-label">Browser</span><select data-test-field="browser" data-test-id="${esc(test.id)}">${selectOptions(BROWSER_OPTIONS,result.browser)}</select></label><label><span class="field-label">Umgebung</span><select data-test-field="environment" data-test-id="${esc(test.id)}">${selectOptions(ENV_OPTIONS,result.environment)}</select></label></div><label><span class="field-label">Tatsächliches Ergebnis</span><textarea data-test-field="actualResult" data-test-id="${esc(test.id)}">${esc(result.actualResult)}</textarea></label><label><span class="field-label">Private Testnotiz</span><textarea data-test-field="note" data-test-id="${esc(test.id)}">${esc(result.note)}</textarea></label><div class="inline-actions"><label class="button button-secondary file-button">Screenshot<input type="file" accept="image/*" multiple data-screenshot-input="${esc(test.id)}"></label>${['failed','partial','blocked'].includes(result.status)?`<button class="button button-danger" data-create-bug-from-test="${esc(test.id)}">Bug erstellen</button>`:''}</div></div></div></article>`}
function renderBugCard(bug){return`<article class="panel compact-card ${String(bug.severity).toLowerCase()==='kritisch'?'panel-danger':''}"><div class="item-head"><div><div class="meta-row"><span class="badge">${esc(bug.id)}</span><span class="badge badge-danger">${esc(bug.severity||'mittel')}</span></div><h4>${esc(bug.title)}</h4><p>${esc(bug.description||'')}</p></div>${statusBadge(bug.status)}</div><select data-entity-status="bugs" data-entity-id="${esc(bug.id)}">${selectOptions([['offen','Offen'],['in-progress','In Bearbeitung'],['testing','Testing'],['blocked','Blockiert'],['behoben','Behoben'],['archived','Archiviert']],bug.status)}</select></article>`}
function renderRiskCard(risk){return`<article class="panel compact-card ${String(risk.severity).toLowerCase()==='kritisch'?'panel-danger':''}"><div class="item-head"><div><div class="meta-row"><span class="badge">${esc(risk.id)}</span><span class="badge badge-danger">${esc(risk.severity)}</span></div><h4>${esc(risk.title)}</h4></div>${statusBadge(risk.status)}</div><div class="kv"><strong>Auswirkung</strong><span>${esc(risk.impact||'—')}</span></div><div class="kv"><strong>Gegenmaßnahme</strong><span>${esc(risk.mitigation||'—')}</span></div></article>`}
function renderAI(){const models=DATA.aiModels.filter(model=>matchesSearch(model.id,model.name,model.role,model.status,model.contexts,model.chats)),chats=flattenProjectChats(DATA),overdue=chats.filter(chat=>['needed','overdue'].includes(String(chat.backupStatus).toLowerCase()));return`${viewHeader('KI / Chats','Modelle, ChatGPT-Projekte, Masterchat, Subchats, Implementierungs-Chats, Backups und Routingregeln.',`<button class="button button-primary" data-open-dialog="backup">Backup-Eintrag anlegen</button>`)}<div class="ai-summary"><section class="panel"><p class="eyebrow">Backup-Gesundheit</p><h3>${overdue.length} Chat(s) mit Handlungsbedarf</h3><p>Backups sind manuell oder ChatGPT-unterstützt. Es gibt keine automatische Hintergrund-Erfassung.</p></section><section class="panel"><p class="eyebrow">Routing</p><h3>${WORKSPACE.workflowTemplates.modelRoutingRules.length} Standardregeln</h3><p>Modellwahl bleibt nachvollziehbar und ersetzt keine Mika-Entscheidung.</p></section></div><section class="stack-section"><div class="section-title"><div><p class="eyebrow">Modelle</p><h3>KI-Hierarchie</h3></div><span class="badge">${models.length}</span></div>${models.map(renderAIModel).join('')||emptyState('Keine KI-Modelle.')}</section><section class="panel"><div class="section-title"><div><p class="eyebrow">Model Routing</p><h3>Standardregeln</h3></div><span class="badge">global</span></div><div class="routing-grid">${WORKSPACE.workflowTemplates.modelRoutingRules.map(rule=>`<article class="routing-card"><span class="id-label">${esc(rule.id)}</span><h4>${esc(rule.workType)}</h4><div class="kv"><strong>Bevorzugt</strong><span>${esc(rule.preferredModel)}</span></div><div class="kv"><strong>Fallback</strong><span>${esc(rule.fallback)}</span></div><p>${esc(rule.rule)}</p></article>`).join('')}</div></section><section class="limitation-panel"><strong>Statische Grenze</strong><p>Die App kann ChatGPT-, Claude- oder Gemini-Chats nicht automatisch auslesen. Sie verwaltet Status, Erinnerungen und Referenzen; der eigentliche Backup-Inhalt wird bewusst manuell oder mit expliziter KI-Unterstützung hinzugefügt.</p></section>`}
function renderAIModel(model){const contexts=asArray(model.contexts),directChats=asArray(model.chats);return`<article class="panel ai-model"><div class="item-head"><div><span class="model-mark">${esc(model.name.slice(0,2).toUpperCase())}</span><div><h3>${esc(model.name)}</h3><p>${esc(model.role||'')}</p></div></div>${statusBadge(model.status)}</div>${contexts.map(context=>`<section class="chat-context"><div class="section-title"><div><span class="id-label">${esc(context.type||'context')}</span><h4>${esc(context.name)}</h4></div>${statusBadge(context.status)}</div>${asArray(context.chats).map(chat=>renderChatCard({...chat,modelName:model.name,contextName:context.name})).join('')||emptyState('Keine Chats in diesem Kontext.')}</section>`).join('')}${directChats.length?`<section class="chat-context direct"><h4>Direkte Modell-Chats</h4>${directChats.map(chat=>renderChatCard({...chat,modelName:model.name})).join('')}</section>`:''}</article>`}
function renderChatCard(chat){const override=asObject(LOCAL.chatOverrides[chat.id]),merged={...chat,...override};return`<article class="chat-card ${['needed','overdue'].includes(String(merged.backupStatus).toLowerCase())?'chat-warning':''}"><div class="item-head"><div><div class="meta-row"><span class="badge">${esc(merged.type)}</span><span class="badge">${esc(merged.id)}</span></div><h4>${esc(merged.name)}</h4><p>${esc(merged.purpose||'')}</p></div>${statusBadge(merged.status)}</div><p class="chat-summary">${esc(merged.currentSummary||'Noch keine öffentliche Zusammenfassung gepflegt.')}</p><div class="chat-backup-row"><label><span class="field-label">Backup-Status</span><select data-chat-backup-status="${esc(merged.id)}">${selectOptions([['current','Aktuell'],['manual','Manuell dokumentiert'],['needed','Backup nötig'],['overdue','Überfällig'],['not-required','Nicht erforderlich']],merged.backupStatus||'needed')}</select></label><div><span class="field-label">Letztes Backup</span><strong>${compactDate(merged.lastBackupAt)}</strong></div><button class="button button-small" data-record-chat-backup="${esc(merged.id)}">Jetzt dokumentieren</button></div><div class="next-action-inline"><span>Nächste Aktion</span><strong>${esc(merged.nextAction||'Keine Aktion gepflegt')}</strong></div></article>`}

'use strict';
function viewHeader(title,description,actions=''){return`<div class="view-head"><div><p class="eyebrow">${esc(DATA?.shortName||WORKSPACE.global.toolName||'KI-Projektmanagement')}</p><h2>${esc(title)}</h2><p>${esc(description)}</p></div>${actions?`<div class="view-actions">${actions}</div>`:''}</div>`}
function emptyState(text){return`<div class="empty">${esc(text)}</div>`}
function listMarkup(items){return asArray(items).length?`<ul class="clean-list">${items.map(item=>`<li>${esc(item)}</li>`).join('')}</ul>`:emptyState('Keine Einträge gepflegt.')}
function metricCard(label,value,detail='',tone=''){return`<div class="metric ${tone?`metric-${tone}`:''}"><span class="metric-label">${esc(label)}</span><strong class="metric-value">${esc(value)}</strong>${detail?`<span class="metric-detail">${esc(detail)}</span>`:''}</div>`}
function progressBar(value,label=''){const numeric=Math.max(0,Math.min(100,Number(value)||0));return`<div class="progress-wrap">${label?`<div class="progress-label"><span>${esc(label)}</span><strong>${numeric}%</strong></div>`:''}<div class="progress"><span style="width:${numeric}%"></span></div></div>`}
function renderWorkspaceDashboard(){const projects=WORKSPACE.projects.filter(project=>matchesSearch(project.id,project.name,project.description,project.statusLabel,project.phase,project.nextAction)),allStats=WORKSPACE.projects.map(project=>projectStats(project)),totalOpenTasks=allStats.reduce((sum,item)=>sum+item.openMika,0),totalOpenBugs=allStats.reduce((sum,item)=>sum+item.openBugs,0),overdue=allStats.reduce((sum,item)=>sum+item.overdueBackups,0),globalProgress=allStats.length?Math.round(allStats.reduce((sum,item)=>sum+item.progress,0)/allStats.length):0;return`<section class="workspace-hero"><div><p class="eyebrow">Workspace</p><h2>Alle Projekte</h2><p>Wähle ein Projekt, um dessen Cockpit, Workflow, KI-Chats, Tests und Backups zu öffnen.</p></div><div class="workspace-actions"><button class="button button-primary" id="workspaceExportButton" type="button">Global exportieren</button><label class="button button-secondary file-button">Global importieren<input id="workspaceImportInput" type="file" accept="application/json"></label></div></section><section class="privacy-banner ${WORKSPACE.global.currentRepositoryVisibility==='public'?'privacy-warning':''}"><div><strong>Private-Repo-Richtung</strong><p>${esc(WORKSPACE.global.privacyNotice||'')}</p></div><span class="badge badge-warning">Aktuell: ${esc(WORKSPACE.global.currentRepositoryVisibility||'unbekannt')}</span></section><div class="global-metrics">${metricCard('Projekte',WORKSPACE.projects.length,'im Workspace')}${metricCard('Ø Fortschritt',`${globalProgress}%`,'projektübergreifend','blue')}${metricCard('Offene Mika-Aufgaben',totalOpenTasks,'alle Projekte','amber')}${metricCard('Offene Bugs',totalOpenBugs,'alle Projekte','red')}${metricCard('Backup-Hinweise',overdue,'Chats mit Handlungsbedarf',overdue?'red':'green')}</div><section class="workspace-search panel"><label><span class="field-label">Projekte durchsuchen</span><input id="workspaceSearch" type="search" value="${esc(currentSearch())}" placeholder="Projektname, Status oder nächste Aktion …"></label></section><section class="project-grid">${projects.map(renderProjectTile).join('')||emptyState('Keine passenden Projekte gefunden.')}</section><div class="workspace-lower-grid"><section class="panel"><div class="section-title"><div><p class="eyebrow">Standardworkflow</p><h3>Lebenszyklus und Freigaben</h3></div><span class="badge">${WORKSPACE.workflowTemplates.lifecyclePhases.length} Phasen · ${WORKSPACE.workflowTemplates.gates.length} Gates</span></div><div class="workflow-strip">${WORKSPACE.workflowTemplates.lifecyclePhases.map(phase=>`<div class="workflow-chip"><span>${esc(phase.order)}</span><strong>${esc(phase.name)}</strong></div>`).join('')}</div></section><section class="panel"><div class="section-title"><div><p class="eyebrow">Backup-Regel</p><h3>Manuell oder ChatGPT-unterstützt</h3></div>${statusBadge(WORKSPACE.global.backupPolicy?.automaticChatIngestion?'automatic':'manual')}</div><p>Spätestens nach etwa ${esc(WORKSPACE.global.backupPolicy?.messageReminderThreshold||15)} Nachrichten sowie vor/nach wichtigen Batches oder Entscheidungen soll ein Backup verknüpft werden.</p><div class="limitation-note"><strong>Technische Grenze:</strong> Die statische App liest ChatGPT-Chats nicht automatisch im Hintergrund ein.</div></section></div>`}
function renderProjectTile(project){const stats=projectStats(project),warning=project.warning||(stats.overdueBackups?`${stats.overdueBackups} Chat-Backup(s) benötigen Aufmerksamkeit.`:'');return`<button class="project-tile" type="button" data-open-project="${esc(project.id)}"><div class="project-tile-head"><div><span class="project-kicker">${esc(project.shortName||project.id)}</span><h3>${esc(project.name)}</h3></div>${statusBadge(project.statusLabel||project.status)}</div><p>${esc(project.description||'')}</p>${progressBar(stats.progress,project.phase||'Fortschritt')}<div class="project-tile-stats"><span><strong>${stats.openMika}</strong>Mika</span><span><strong>${stats.openBugs}</strong>Bugs</span><span><strong>${stats.openQuestions}</strong>Fragen</span><span><strong>${stats.overdueBackups}</strong>Backups</span></div><div class="project-next"><span>Nächste Aktion</span><strong>${esc(project.nextAction||'Noch nicht festgelegt')}</strong></div><div class="project-meta"><span>Update ${compactDate(project.lastUpdated)}</span><span>Backup ${compactDate(project.lastBackupAt)}</span></div>${warning?`<div class="project-warning">⚠ ${esc(warning)}</div>`:''}</button>`}
function renderProjectDashboard(){const stats=projectStats(DATA),lifecycle=WORKSPACE.workflowTemplates.lifecyclePhases.find(item=>item.id===DATA.workflowState.currentLifecyclePhaseId),activeGate=DATA.gates.find(gate=>!['approved','done'].includes(String(LOCAL.gateOverrides?.[gate.id]?.status||gate.status).toLowerCase()))||DATA.gates.at(-1),timeline=combinedTimeline().slice(0,6),chats=flattenProjectChats(DATA),backupChats=chats.filter(chat=>['needed','overdue'].includes(String(chat.backupStatus).toLowerCase())).slice(0,4);return`${viewHeader('Projektcockpit','Projektstatus, nächste Aktion, Workflow-Gate, Qualität und KI-Backupzustand auf einen Blick.',`<button class="button button-secondary" data-project-export>Projekt exportieren</button>`)}${DATA.warning?`<div class="project-alert"><strong>Wichtiger Hinweis</strong><p>${esc(DATA.warning)}</p></div>`:''}<div class="project-summary-grid"><section class="project-summary-main panel"><div class="section-title"><div><p class="eyebrow">Aktuelle Phase</p><h3>${esc(DATA.phase||lifecycle?.name||'Nicht gesetzt')}</h3></div>${statusBadge(DATA.statusLabel||DATA.status)}</div>${progressBar(stats.progress,'Projektfortschritt')}<div class="next-action-box"><span>Nächste empfohlene Aktion</span><strong>${esc(DATA.nextAction||'Noch nicht festgelegt')}</strong></div></section><section class="metric-grid compact">${metricCard('Batch-Aufgaben',`${stats.taskDoneCount}/${stats.tasks.length}`,'erledigt')}${metricCard('Tests',`${stats.testsDone}/${DATA.tests.length}`,'abgeschlossen','blue')}${metricCard('Mika-Aufgaben',stats.openMika,'offen','amber')}${metricCard('Bugs',stats.openBugs,'offen','red')}</section></div><div class="dashboard-grid"><section class="panel"><div class="section-title"><div><p class="eyebrow">Gate</p><h3>${esc(activeGate?.name||'Kein Gate gepflegt')}</h3></div>${activeGate?statusBadge(LOCAL.gateOverrides?.[activeGate.id]?.status||activeGate.status):''}</div><p>${activeGate?.requiresMikaApproval?'Mika-Freigabe ist für dieses Gate vorgesehen.':'Keine explizite Mika-Freigabe markiert.'}</p><button class="text-link" data-project-view-jump="workflow">Phasen und Gates öffnen →</button></section><section class="panel ${backupChats.length?'panel-warning':''}"><div class="section-title"><div><p class="eyebrow">KI / Chats</p><h3>Backup-Status</h3></div><span class="badge ${backupChats.length?'badge-danger':'badge-success'}">${backupChats.length} offen</span></div>${backupChats.length?backupChats.map(chat=>`<div class="compact-row"><div><strong>${esc(chat.name)}</strong><span>${esc(chat.modelName)}</span></div>${statusBadge(chat.backupStatus)}</div>`).join(''):emptyState('Alle erfassten Chat-Backups sind aktuell oder manuell dokumentiert.')}<button class="text-link" data-project-view-jump="ai">KI & Chats öffnen →</button></section><section class="panel dashboard-timeline"><div class="section-title"><div><p class="eyebrow">Timeline</p><h3>Letzte wichtige Änderungen</h3></div><button class="button button-small" data-project-view-jump="knowledge">Alle anzeigen</button></div>${timeline.map(renderTimelineItem).join('')||emptyState('Keine Timeline-Einträge.')}</section><section class="panel"><div class="section-title"><div><p class="eyebrow">Schnellerfassung</p><h3>Private Notiz oder Aufgabe</h3></div><span class="badge">nur lokal</span></div><textarea id="dashboardQuickNote" placeholder="Beobachtung, Entscheidungsvorlage oder nächste Aktion …"></textarea><div class="inline-actions"><select id="dashboardQuickNoteType"><option value="note">Notiz</option><option value="task">Mika-Aufgabe</option><option value="idea">Idee</option><option value="question">Frage</option></select><button class="button button-primary" id="saveDashboardQuickNote">Speichern</button></div></section></div>`}
